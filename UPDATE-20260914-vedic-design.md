# 印度占星重設更新包

本包只包含本輪新增或修改的檔案，請覆蓋至 **2026-09-13 印度占星更新版**，保持 ZIP 內相對路徑。這不是完整網站原始碼包。

## 本次更新

- 自有出生日期／24 小時編輯器，含觸控滾輪、年份快速輸入、鍵盤操作及閏年驗證。
- 命盤／九曜／運期／解讀四個分頁；北印度紙面命盤、南式切換及完整十六分盤。
- 獨立印度花瓣拱與 3D 星殿：27 月宿 → 原生九曜定位 → 北印度命盤；可返回、略過或使用減少動態模式。
- 修正 Gaja Kesari 只檢查月木角宮的問題，補齊本版採用的吉曜支持、落陷、燃燒角距與合成友敵條件。
- 補入六曜燃燒角距與水／金星順逆行差異；速度改為前後 10 分鐘中心差分。
- 出生時間誤差傳入交運日期取樣，副運選擇確實展開各自的次副運。
- 八種解讀方向使用同一份完整原生資料，納入新條件及時間敏感度。

## 核對結果

全站 `npm test` 通過；印度占星既有 14 組、新增 13 組核心測試通過。167 組標準星曆、72 組高緯上升、6 組 Raman 參照持續相符於記錄的容差內。Chromium 觸控模擬／桌面完成 25 項流程，包含 320px 窄螢幕、滾輪、雙向滑動、取消與略過、WebGL fallback、夏令時間錯誤及未知時間。

詳細來源、算法、實測誤差與測試範圍：`docs/vedic-design-20260914.md`。截圖、原生命盤、完整提示詞範例及測試記錄：`review/vedic-design-20260914/`。

## 套用

1. 保留原網站備份，把本包依原目錄覆蓋。
2. 一併更新 `index.html`、根目錄及 `JS` 目錄下的 `sw.js`；本版快取為 `jy-main-v80`。
3. 重新載入網站。入口可直接使用，不需要另接天文或 AI 付費 API。

打包清單 `CHANGE-MANIFEST-20260914-vedic-design.json` 記錄每個檔案的本輪前後 SHA-256。`JS/vedic-scene.js` 已完成建置，原始碼及重建指令亦保留。新增梵文字型附原授權。

此版本明確採用 Lahiri／Raman、整宮制、平均交點、指定 Parashari 分盤與 Vimshottari 方法。文獻與數值核對用來提高實作的一致性，不表示所有流派或現實事件預測已獲證實。
