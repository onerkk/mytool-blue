# 開始洗牌無反應修正
版本：20260911shuffle3

適用於已更新月下沉浸儀式版本的網站。本包僅包含此次 8 個新增／修改檔，請依 ZIP 原路徑覆蓋網站根目錄，完成後重新整理網站。既有場景圖片與 CSS 不需重傳。

修正：
- 底部「開始洗牌」改為直接啟動儀式，不再透過隱藏的 jy-shuffle-btn 轉送點擊。
- 舊洗牌按鈕也轉用同一入口，避免各自保留初始化時的舊狀態。
- 儀式完成後才開放選牌；開始洗牌不會提前抽牌或顯示牌義。
- 返回取消、切換牌陣仍會讓前一輪回呼失效；連點不會啟動兩個儀式。
- 元件未載入或牌組未就緒會顯示並捲至錯誤提示。
- 一併修正快速揭牌傳入牌名的欄位：優先使用牌資料實際的 n 欄位。
- 腳本引用版本同步更新，Service Worker 快取升為 v68。

驗證：用上一版入口執行新增回歸案例，重現「缺少隱藏舊按鈕時無法啟動」；同一案例在修正版通過。最終儀式流程 25 組、介面流程 7 組測試通過，修改的 JavaScript 語法檢查通過。包含無隱藏按鈕、舊處理函式失效、取消再啟動、元件缺失、切換牌陣取消等情況。

上述為實際生產函式搭配模擬 DOM 的測試，不代表已在使用者手機或正式網站驗收；沒有取得此次手機瀏覽器的錯誤紀錄，因此不能排除部署端另外存在檔案未完整更新的情況。
