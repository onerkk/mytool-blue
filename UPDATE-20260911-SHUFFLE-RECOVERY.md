# 洗牌原頁重試修正 · 20260911shuffle5

不需要修改問題才能洗牌。舊版「請返回修改問題後重試」是把洗牌啟動錯誤導向輸入頁的錯誤提示，並不是占卜規則，也不是問題內容驗證。

## 套用

本包僅含相對上一份 `jingyue-cinematic-upgrade.zip` 的新增／修改檔案。解壓縮後，把檔案依原路徑覆蓋到網站專案；不要移除其他檔案。已有角色、場景、牌圖不需重新上傳。

部署時請一起更新 `index.html`、`JS/sw.js` 與本包全部 JavaScript。已包含編譯好的 `JS/cinematic-stage.js`，靜態網站無需另外安裝套件或執行建置。部署完成後重新整理網站，以載入 `20260911shuffle5`；更新前已開啟的分頁仍執行舊程式。這份檔案交付沒有代替你部署正式站。

## 修正內容

- 洗牌啟動失敗時，按鈕顯示「重試洗牌」，保留問題、牌陣與已準備好的牌組，不強迫返回輸入頁。
- 如果牌組初始化在中途失敗，下次按鈕會重新完成準備，不會因為已有部分資料而跳過初始化。
- 儀式只有準備好後才取得進行中狀態；掛載中斷會移除對話框、還原頁面捲動與事件監聽，釋放狀態，允許再次啟動。
- 首頁特效暫停／恢復、場景階段更新、手勢互動或 3D 資源釋放出錯時，不再阻斷核心流程。畫面保留插畫版儀式與操作按鈕，不會直接算作已完成洗牌。
- 洗牌完成只開放選牌，不會提前抽牌或顯示解讀。返回、取消及已失效的完成回呼仍維持原有資料隔離。
- 更新四個前端程式的版本參數及 Service Worker 快取版本 `jy-main-v70`。

## 實際核對與限制

已在正式網站瀏覽器操作「輸入工作問題 → 手動選凱爾特十字 → 抽牌解讀 → 開始洗牌 → 走進神殿 → 啟動 → 開始選牌」。沒有修改問題，回到選牌頁時為 0 / 10 張，按鈕已轉為快速抽牌；該次沒有 `[Tarot shuffle]` 錯誤。

測試瀏覽器無法建立 WebGL context，因此這次實站操作驗證的是既有版本的插畫備援流程，**沒有重現你手機上的啟動例外，也沒有驗證手機 GPU 效果**。不能僅依截圖把確切原因說成某一款手機或某個 WebGL 錯誤。

本次修正另外使用實際程式配合模擬 DOM 與故障注入驗證：

| 測試 | 結果與範圍 |
| --- | --- |
| `tests/shuffle-recovery-20260911.cjs` | 10 組：首頁特效失敗、場景初始化、掛載前／後中斷、舊版呼叫中斷、牌組準備中斷、七種儀式資源清理、手勢中途失效、首頁場景恢復、Three.js 資源釋放。 |
| `tests/ritual-lifecycle-20260911.cjs` | 25 組通過：儀式完成／取消、重複點擊、牌面顯示時機、牌陣切換、各命理原始資料及回呼。 |
| `tests/cinematic-20260911.cjs` | 10 組通過：場景與手勢、無 WebGL 備援、資源清理、逐步表單保留。 |
| `tests/atelier-ui-20260911.cjs` | 7 組通過：入口、牌陣選擇、返回、問題與表單保留。 |

將相同的 10 組失敗恢復測試放到上次交付的對應程式碼，10 組皆失敗；本次修正後全部通過。這證明的是錯誤恢復路徑獲得修補，並不是已取得手機錯誤堆疊，也不是全機型保證。

修補版尚未在正式站或實體手機上操作。套用後可沿原問題直接測試洗牌；若仍發生錯誤，需以當次瀏覽器主控台 `[Tarot shuffle]` 記錄定位剩餘原因。

本次新增測試可執行 `npm run test:shuffle-recovery`，也已接入既有 `npm test` 測試鏈。全部修改均集中在洗牌恢復與動畫生命週期，不更動命理計算和提示詞。
