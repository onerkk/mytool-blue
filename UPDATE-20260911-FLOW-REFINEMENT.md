# 靜月之光 · 閱讀動線與共用介面更新

版本：20260911flow6。這是相對目前 20260911shuffle5 工作版本的變更檔更新包，保留已加入的人物、場景、3D 演出與洗牌重試保護。

## 安裝

解壓後，將 `index.html`、`JS/`、`CSS/` 等內容依原路徑覆蓋到目前專案。新增的 `CSS/flow-refinement.css` 必須一併上傳；新版 index 已將它放在現有樣式最後載入。根目錄 `meihua-standalone.js` 與 `JS/meihua-standalone.js` 已同步。

檔案包含原始碼與測試，無需重新建立人物或 Three.js 圖像素材。JS 查詢版本為 `20260911flow6`，Service Worker 快取版本為 `jy-main-v71`。本次未部署至線上網站。只覆蓋單一 HTML 或 CSS，會造成版本不完整。

## 變更

- **真實手勢**：儀式互動區的 `touch-action` 不再被共用按鈕樣式覆蓋。拖曳會立即改變場景輸入；無 WebGL 時，插畫牌組也會跟隨手指移動。保留長按與直接啟動入口。
- **選牌控制**：以一套 Pointer Events 同時處理滑鼠與觸控。辨識水平拖曳與垂直捲動，拖曳結束不誤選牌；移除重複觸控監聽、持續自動漂移與慣性計時器。補上左右瀏覽鍵及鍵盤方向鍵。
- **閱讀順序**：儀式先顯示說明，再呈現互動區，最後顯示主要按鈕。洗牌／選牌操作回到牌桌內，避免固定底部欄遮住內容。洗牌前收起選牌區，完整牌位圖仍可自行展開。
- **捲動行為**：移除選擇工具後的延遲捲動、進入問題頁時的文字框自動聚焦，以及數個完成後跳往下方按鈕的延遲動作。明確按下一步或切換頁面時，才定位新步驟。雷諾曼、梅花、八字和合盤在原頁重繪時保留捲動位置；展開全部欄位的選擇也會保留。
- **共用 UI**：重新設定返回鍵、主要操作、次要連結、選取狀態、欄位、選擇器、步驟列與結果面板。縮短人物標頭，標題使用完整行寬；金色主要動作與一般選項分開呈現。既有蝦皮入口保留。
- **結果與 AI**：先顯示三個 AI 入口，其餘透過原生可展開區域選取；移動原本節點，保留各自處理函式。塔羅／開鑰的 AI 分頁在點擊當下開啟，避免等待剪貼簿後被擋；複製失敗時可展開本次完整文字手動複製。

本次沒有修改排盤曆法、牌陣張數、牌位定義、抽牌隨機來源或提示詞中的占卜資料與讀法。

## 已執行的驗證

| 測試 | 通過組數 | 範圍 |
| --- | ---: | --- |
| `npm run test:flow` | 14 | 手勢、誤觸、垂直捲動、左右鍵、重複初始化、閱讀順序、表單位置、AI 失敗處理 |
| `npm run test:ui` | 7 | 八個入口、返回、牌陣選擇器、雷諾曼牌序、表單保留 |
| `npm run test:ritual` | 25 | 儀式生命週期、取消、提前解讀防止、命理結果延後提交 |
| `npm run test:cinematic` | 10 | 演出狀態、原始場景資源釋放、逐步填寫 |
| `test:cinematic` 內含洗牌復原測試 | 10 | GPU 與啟動失敗、原頁重試、清理與取消 |

合計 66 組。另檢查變更 JS 的語法、CSS 解析、HTML 控制項 ID、牌桌內按鈕位置與載入版本。這些主要是在模擬 DOM / 時鐘環境中執行，並不等於手機瀏覽器操作或畫面驗收。

## 尚未完成的驗證

本次本地預覽服務可啟動，但瀏覽器無法連入預覽，停留在空白頁。因此**尚未完成此版的手機／桌面畫面檢查、實體觸控拖曳、不同螢幕下的文字與 3D 效果驗收**；不能宣稱每個裝置都已實測或「全部完美」。

部署後應在實際手機確認：同一問題可直接洗牌、左右拖曳與瀏覽鍵都能操作、向上／下捲動不選牌、切換工具與表單選項不跳頁、抽滿後才能取得解讀，以及 AI／下載／返回行為。這是需要實機確認的部分，並非要求使用者修改問題才能繼續。
