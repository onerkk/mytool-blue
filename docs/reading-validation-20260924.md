# 2026-09-24 驗證紀錄

## 實際執行

`package.json` 的 `test` 任務遞迴展開為 **71 項命令**，逐項執行全部以 exit code 0 完成。完整命令、所屬任務及耗時保留於 [JSON 紀錄](reading-validation-20260924.json)。本次也核對紀錄與目前 `package.json` 配置逐項相同，沒有跳過失敗項或只保留成功結果。

早期執行抓出共用文案縮短後的原有契約字句差異，已保留相應語義與相容字句後重跑。新增的實際入口檢查另外抓出姓名筆畫未輸出、紫微尾行位置不一致，修正後已通過。

| 新增／重點檢查 | 結果 |
| --- | --- |
| 八字／紫微原始案例 | 四柱時辰、雙向配對、四化來源、運限範圍與資料不變；8 組通過 |
| 新作答流程 | 14 方法 × 6 題型、子題、健康辨識、失敗答案、參考答案與原盤修訂；8 組通過 |
| 實際匯出整合 | 14 種方法的原生計算與實際提示詞函式；5 組通過 |
| 同步與備援 | 共用文字、獨立備援與根目錄鏡像相同 |

新流程整合測試同時覆蓋原有 16 組牌陣／排盤測試；這些組數是測試組織方式，不是命理準確率。

## Chromium 手機操作

使用 390 × 844 視窗執行實際頁面，以下均通過：

- 合盤：輸入案例資料、產生結果、複製提示詞、下載 JSON，確認兩者相符且原盤不變。
- 印度／西洋占星：真正入口完成排盤、複製與下載，確認原始星盤不變。
- 免費流程：保留舊登入／定價狀態仍不啟動付費 AI，瀏覽次數 API 以本機回應模擬。
- 解讀回看：14 方法選單、失敗案例提示、人工參考不被認證成「正確」、加入原盤後重答與複製、修改後清除舊結果、清空內容、無橫向溢出。
- 回看頁沒有資料上傳或瀏覽器儲存；輸入的 HTML 字樣按文字處理。

容器未具備中文字型，因此回看頁截圖不列入完整字型渲染驗收；文字內容與版面尺寸透過實際 DOM 核對。本次沒有聲稱已在真實 iPhone／Android 裝置上逐一測過。

## 沒有冒充完成的驗證

沒有呼叫付費模型，也沒有把人工寫的答案冒充 ChatGPT／Claude 的生成結果。`review()` 是已知問題的本機模式檢查，不能驗證所有語義、命理取捨、預測能力或導購效果；即使沒有標記問題，也要求人工核對。原生引擎採用的流派政策與資料精度仍保留在各自輸出內。

ZIP 在交付前另核對：相對路徑、無重複項、所有成員可解壓、內容與工作檔一致，且每個收錄檔案都確實相對原始專案有變更或為新增檔案。
