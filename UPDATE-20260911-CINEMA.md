# 靜月之光 · 月下問心更新

版本：`20260911cinema4`。本包整合先前的儀式更新、洗牌修正與本次角色／3D 升級，只收錄相對前次改版基底新增或有變動的檔案。它是覆蓋更新包，需保留網站原有的牌圖、資料與其餘程式，不是可單獨部署的完整網站。

## 套用

解壓縮後，將內容依原本路徑合併至網站根目錄，覆蓋同名檔案。`index.html`、`CSS`、`JS`、`assets` 必須一起更新。已附建置好的 `JS/cinematic-stage.js`，現有靜態網站不需要新增 CDN 或在伺服器執行 Node。

主程式版本已同步更新，Service Worker 快取改為 `jy-main-v69`。本說明取代本包較早的 RITUAL／SHUFFLE 說明中的版本與檔案數；兩份舊說明僅保留更新歷程。

## 這版實際改了什麼

首頁改為角色與場景構成的入口。月見、星衡、清和三位原創引路角色各有迎接、凝心、啟動、揭曉四種姿態；在儀式裡隨流程切換，並加入衣袖微動、呼吸與視角回應。首頁在可見時才建立 3D 場景，離開畫面或進入儀式便釋放，避免首頁與儀式同時渲染。

**角色是多姿態動畫插畫，不是完整骨架 3D 人物，也不是影片。** 牌組與儀式器物則使用 Three.js / WebGL2 建立真正的幾何、深度、材質與即時光照。沒有拿參考網站的人物或影片重製成自己的素材。

| 探索方式 | 本次演出與互動 | 資料處理 |
| --- | --- | --- |
| 塔羅快讀 | 月見陪同；左右切牌／長按，3D 牌組分疊、交錯、展扇；快速抽牌後逐張翻面 | 沿用本次牌組、牌序、正逆位；洗牌儀式不另抽牌 |
| 雷諾曼 | 月見陪同；月庭光場，親手揭開本次前 3 張牌 | 超過 3 張的牌陣保留在完整結果；不把其他牌陣當三張牌解讀 |
| 八字 | 星衡陪同；依序點亮年、月、日、時與 3D 四柱器物 | 儀式只呈現四個時間座標，不預先編造干支 |
| 八字合盤 | 星衡陪同；兩人分別點亮，雙星軌道靠近 | 雙方原始資料、真太陽時與實際合盤計算不受演出影響 |
| 紫微斗數 | 星衡陪同；拖動星儀或長按，3D 星軌與十二個光點啟動 | 儀式中的光點是視覺座標；實際星曜配置以計算後的命盤為準 |
| 梅花易數 | 清和陪同；拂過光線或長按，六道光環隨手勢轉動 | 不用裝飾圖案冒充本卦、互卦或變卦 |
| 靜月靈籤 | 清和陪同；上下輕晃／長按，籤筒與籤枝回應；進入原有擲筊、確認流程 | 保留原求籤、擲筊與連續聖筊規則 |
| 開鑰之法 | 輸入頁接入月見；原有五層操作、動畫與解讀程序保留 | 未將原程序改成通用洗牌或新造牌陣 |

所有手勢都有按鈕替代操作；儀式可返回、跳過、使用 Escape，聲音需主動開啟。系統設定減少動態時保留操作步驟，停止持續動畫。沒有 WebGL2 或發生繪圖環境中斷時，保留角色插畫及可操作的原有器物控制。

雷諾曼、八字快排、紫微、梅花、八字合盤的輸入改為逐步填寫，也可展開所有欄位。前後切換保留原欄位與值；切換牌陣／方式造成原引擎重繪時，會回到同一步。提交前展開各區，確保原引擎的資料錯誤提示不會藏在前一步。

首頁、各探索入口與結果區加入同一套場景語彙。返回按鈕與蝦皮導覽保留；結果末尾新增可展開的日常選物入口，不以購買改運、保證感情或預測成功作承諾。命理提示詞和選陣引擎沿用目前專業版，本次演出不取代它們。

## 驗證範圍

`npm test` 全部通過，包含既有命理資料／提示詞、56 個選陣案例、抽牌狀態、分享卡、API 失敗處理與 UI 回歸。既有儀式 25 組，加上本次 cinematic 10 組均通過。

本次新增測試覆蓋：手勢方向與取消、洗牌分疊幾何不使用抽牌亂數、引路角色切換、分步欄位保留、重繪保留牌陣選擇步驟、WebGL 無法啟動時可繼續、七種場景的幾何／材質／貼圖／監聽器／動畫清理、關閉後才完成的貼圖載入不會重新顯示場景。成功渲染流程使用真實 Three.js 幾何搭配渲染器替身驗證資源生命週期，沒有冒充 GPU 畫面驗收。

`npm run build:cinematic` 完成；修改的 JS／CSS 可解析，頁面腳本順序、版本與本地素材路徑已核對。最後的構圖比例調整後，重新執行 cinematic 10 組及建置成功。

**尚未完成：新版瀏覽器畫面驗收、實機手機觸控、GPU shader 像素輸出、音效聽感與實際幀率。** 本次預覽服務啟動成功，但瀏覽器持續停在空白分頁，無法開啟新版。沒有因此宣稱「每個實機流程都操作過」「已超越參考站」或「保證增加蝦皮曝光」。先前在參考站觀察的操作，也不能代替新版驗收。

維護者後續驗收優先項目：390px 與 360px 手機中角色頭部與按鈕不被遮擋；真實洗牌後才可選牌；退出再進入不殘留牌面；全部七種儀式的手勢／替代按鈕／返回；大牌陣與正逆位；分步表單的日期及地點錯誤；分享預覽與下載；低效能及減少動態模式。

## 建置與素材

需要改動 3D 程式時，在原專案執行 `npm ci`，再執行 `npm run build:cinematic`。原始碼在 `JS/cinematic-src/`；完整 Three.js 已打包，不向外部 CDN 載入。MIT 授權保留於 `licenses/three-MIT.txt`。

三位角色皆為本次原創生成的 4 格透明圖集，保存為 WebP；原始圖集比例為 1774 × 887。圖像沒有經人為去背或改畫，僅做保留透明度的格式壓縮。角色姿態切換採用圖集分欄，邊緣取樣留有細小內縮避免串格。

參考觀察：[MZ 韓國範山道令](https://www.sajutight.me/mzmudang-tw) 的對話與資料填寫段有正在播放的循環人物影片。已觀察至要求電子郵件的入口，沒有提交電子郵件或購買服務。

工程依據：[Three.js WebGLRenderer 官方文件](https://threejs.org/docs/pages/WebGLRenderer.html) 的 WebGL2 渲染、像素比例與資源釋放；[MDN WebGL 最佳實務](https://developer.mozilla.org/en-US/docs/Web/API/WebGL_API/WebGL_best_practices) 的裝置負載與資源管理。以上是實作參考，不是網站效果或命理準確性的背書。
