# 靜月之光：核心引擎與提示詞更新

本包合併 2026-09-13 的提示詞重寫與後續核心修正，只含變更檔案和新增的查核、測試及審閱資料。

## 套用方式

1. 基準為 `onerkk/mytool-blue` 提交 `74b27e3004fde0c26a030f4bb6835a57ba7ea9f3`。原上傳 ZIP 已無法在工作區讀取，本次從該版本恢復原始碼，沒有宣稱與原附件完成逐檔比對。
2. 備份網站專案後，將 ZIP 內檔案按原路徑覆蓋到專案根目錄。若已套用前一份 `jingyue-methods-update-20260913.zip`，可直接再套本包。若基準不同，先比較變更清單，避免蓋掉其他修改。
3. 保留 `JS/`、`functions/`、`tests/` 等目錄層級。本包是差異包，不能單獨當完整網站啟動；未變更素材、依賴和既有檔案須保留。
4. 使用原部署流程發佈後重新載入。核心腳本版本為 `20260913core2`，Service Worker 為 `jy-main-v77`。本次只交付檔案，沒有替你發佈。

## 主要變更

- 八字：修正四支漏根、旺相混淆、中和被當身弱、合化候選誤導；扶抑、調候與全局通路使用同一份四柱事實。120 個調候入口逐干核對透藏和牽制，移除說明文字觸發的歲運吉凶加分。
- 使用者盤 `癸亥／庚申／乙酉／癸未`：月令取格顯示正官格；依已宣告政策顯示「本盤不採真化金格」，並列亥甲、未乙與兩癸等理由。調候明列癸已透、丙未見，避免只有「待覆核」而沒有實際分析資料。
- 紫微：修正曆法入口，保留十二宮／四化的本命與運限身分，新增具名政策的大限與流年流曜，出盤前檢查完整性。安星位置沿用已通過獨立盤例的規則。
- 塔羅、開鑰、雷諾曼及靈籤：修正整數隨機取樣；開鑰四堆改由兩層切牌產生；靈籤明定數位擲筊政策。梅花及各牌陣持續以實際幾何、體用互變和程序狀態輸出。
- 十二類提示詞：先作有依據的主判，再深入說明組合、位置、發展條件和行動。客製手鍊報告共用核心資料，不再另跑補缺百分比或宣稱雙系統證實材料效果。
- 手機功能：重新驗證五連點隱藏計數器、四柱立體儀式、第一輪停止後查看原因及上下觸控滑動。這些功能已存在於恢復的基準版本；本包不重複收錄未修改的視覺素材。

## 查核與測試

完整 `npm test` 通過；包含 160 張獨立紫微固定盤的 18,560 項比對，以及曆法邊界、64 卦×6 動爻、牌陣幾何和程序測試。本輪另有七組合化反例／資料傳遞測試及十組核心修正測試。手機尺寸瀏覽器五條流程通過，開鑰結果畫面可上下原生觸控滑動。

- `docs/core-engine-audit-20260913.md`：本輪核心問題、文獻連結、所選政策、反例與未實作範圍。
- `docs/method-sources-20260913.md`：各方法的來源和提示詞重寫依據。
- `review/core-20260913/`：最新完整測試日誌、瀏覽器結果、截圖、120 個調候條件及實際提示詞。
- `review/huage-20260913/`：使用者四柱的合化評估及單項提示詞。
- `review/methods-20260913/`：十二類方法文字、各系統實際提示詞和初輪查核紀錄。
- `review/core-20260913/changed-files.json`：本差異包的檔案清單、基準與 SHA-256；清單本身不自列雜湊。

旺衰加權仍是本站相對模型，調候並未完成古籍所有分支的自動裁決；紫微也未新增全部門派的流日、流時細則。包內逐項交代已測規則和範圍，不將文獻查詢或測試通過寫成全部判法均已證實。

外部 AI Worker 原始碼不在本專案；已更新本地 API 和送出的資料／方法包，但沒有部署或修改外部 Worker，也未呼叫付費模型驗證最終成文。
