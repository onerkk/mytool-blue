# 印度占星・九曜星殿更新

這個 ZIP 只含本輪新增或變更檔案，基準是上一輪 `jingyue-engine-output-fix-20260913.zip` 套用後的網站。

請把 ZIP 內相同相對路徑的檔案覆蓋／加入網站根目錄，包含 `index.html`、`JS`、`CSS`、`assets/ui`。不需更改 Worker、資料庫或環境變數。

首頁新增「印度占星」入口，可使用：

- 地心恆星黃道九曜、Lahiri／Raman、IANA 出生時區與時間敏感度。
- 十六分盤，南／北印度式切換，宮主、相位、九曜詳情。
- Vimshottari 大運／副運／次副運、BAV／SAV、三年月度行運快照。
- 八種深入提示詞方向，含配戴與工作問題。
- 3D 九曜星儀、入殿過場、按鍵回饋、分盤轉場；可略過或切靜態模式。
- 命盤 JSON 下載及完整提示詞複製。

詳細政策、來源、文獻筆誤、精度對照與尚未實作模組見 `docs/vedic-engine-20260913.md`。本版不宣稱囊括所有印度占星流派；歷史七維綜合頁也不等同新獨立核心。

新 Service Worker 版本為 `jy-main-v79`，會更新舊版主站快取。若主機另有 CDN 快取，更新後請重新載入，核對首頁是否出現印度占星入口。

`review/vedic-20260913` 和 `tests` 是可核對的測試與截圖，不是執行網站必需資料；`JS/vendor/astronomy-engine-LICENSE.txt` 請保留。
